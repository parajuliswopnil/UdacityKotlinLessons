fun main(args: Array<String>) {
        println("hello, ${args[0]}")

    val isUnit = println("the value of println is ")
    println(isUnit)

    val temperature = 10
    val ishot = if (temperature > 50) true else false
    println(ishot)
    val message = "The fish is ${if (temperature > 50)"fried" else "safe "+"fish"}"
    println(message)

}